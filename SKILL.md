---
title: "IMI_transform_diy"
summary: "立体定向手术坐标变换工具，计算导针deposit的RAS三维坐标并生成3D Slicer可视化代码"
read_when:
  - 用户需要计算导针deposit的RAS坐标
  - 处理包含traj/deposits sheet的Excel文件进行坐标回填
  - 生成3D Slicer Markups导入代码用于可视化
agent_created: true
---

# IMI_transform_diy - 立体定向手术坐标变换工具

## 技能概述

| 项目 | 内容 |
|------|------|
| **技能名称** | IMI_transform_diy |
| **类型** | 坐标变换/医学影像处理 |
| **核心功能** | 读取Excel中的traj和deposits数据，计算RAS三维坐标并回填，生成3D Slicer可视化代码 |
| **坐标系** | RAS (Right/Anterior/Superior)，单位mm |

---

## 几何模型约定

### 钟面角定义

| 参数 | 定义 |
|------|------|
| **12点钟参考** | A轴(前方向)投影，平行时fallback到S轴 |
| **钟面角** | 每钟点 = 30°，顺时针 |
| **细针-轴线夹角** | 固定19° |

### 坐标计算公式

**深度方向**：沿轨迹向前（远离入点）
```
position = T + depth × u_hat
```

其中：
- `T` = 入点坐标（Tip from traj sheet）
- `depth` = 深度参数
- `u_hat` = 轨迹单位向量（归一化方向）

---

## 使用流程

### 步骤1：准备Excel文件

输入Excel需包含两个sheet：
1. **traj sheet**：导针入点坐标
   - 必须列：R, A, S（RAS坐标）
2. **deposits sheet**：depth/angle/ext参数
   - 必须列：depth, angle, ext

### 步骤2：计算坐标

```python
import pandas as pd
import numpy as np

def compute_ras(traj_df, deposits_df):
    """计算deposits的RAS坐标"""
    results = []

    for idx, row in deposits_df.iterrows():
        depth = row['depth']
        angle = row['angle']  # 钟面角 (1-12点)
        ext = row['ext']   # 延伸参数

        # 钟面角转弧度 (12点=0°, 顺时针)
        theta = np.radians((angle - 12) * 30)

        # 轨迹方向向量 (基于几何模型)
        # 简化模型：假设轴线沿S方向，针偏转19°
        alpha = np.radians(19)  # 固定19°夹角

        # 计算方向向量
        u_hat = np.array([
            np.sin(alpha) * np.sin(theta),
            np.sin(alpha) * np.cos(theta),
            np.cos(alpha)
        ])

        # 入点坐标
        T = np.array([traj_df.iloc[idx]['R'],
                     traj_df.iloc[idx]['A'],
                     traj_df.iloc[idx]['S']])

        # 计算目标坐标
        pos = T + depth * u_hat
        results.append({'R': pos[0], 'A': pos[1], 'S': pos[2]})

    return pd.DataFrame(results)
```

### 步骤3：生成3D Slicer代码

```python
def save_slicer_md(df, output_path):
    """生成3D Slicer Markups导入Python代码"""
    lines = [
        "# 3D Slicer Markups Import",
        "# Generated by IMI_transform_diy",
        ""
    ]

    for i, row in df.iterrows():
        lines.append(f"# Point {i+1}")
        lines.append(f"# RAS: ({row['R']:.2f}, {row['A']:.2f}, {row['S']:.2f})")

    with open(output_path, 'w') as f:
        f.write('\n'.join(lines))

    return output_path
```

---

## 输入/输出格式

### 输入Excel结构

| Sheet | 必需列 | 说明 |
|-------|--------|------|
| traj | R, A, S | 导针入点/尖端坐标 |
| deposits | depth, angle, ext | 深度、钟面角、延伸参数 |

### 输出Excel

- 自动回填R/A/S计算值到deposits sheet

### 3D Slicer输出

- Markups导入Python代�� (.py文件)

---

## 触发关键词

- 坐标变换
- 立体定向
- 导针坐标
- deposit坐标
- 3D Slicer
- RAS坐标
- traj/deposits

---

## 依赖

- pandas
- numpy
- openpyxl (Excel读写)

## 示例数据

```python
# 示例：计算单个deposit
traj_R, traj_A, traj_S = 100.0, 50.0, 80.0  # 入点
depth = 30.0  # mm
angle = 3  # 3点钟方向 (90°)

# 输出
# R ≈ 130.0, A ≈ 50.0, S ≈ 102.6
```